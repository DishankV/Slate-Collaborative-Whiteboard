# Server
PORT=4000
NODE_ENV=development

# MongoDB (use MongoDB Atlas in production - never commit real credentials)
MONGO_URI=mongodb://mongo:27017/whiteboard

# Comma-separated list of origins allowed to call the API / connect via sockets
CLIENT_ORIGIN=http://localhost:5173

# Salt rounds for board password hashing
BCRYPT_SALT_ROUNDS=10
